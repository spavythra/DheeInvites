{
  "name": "dheeinvites",
  "version": "1.0.0",
  "description": "Zero-dependency event invite template — visual regression tests via Playwright",
  "scripts": {
    "test": "playwright test",
    "test:update": "playwright test --update-snapshots"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  }
}
